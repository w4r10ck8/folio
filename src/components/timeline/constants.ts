export const DOT_SEAM_RATIO = 0.15; // 50% - 35% column split — dot sits on the card boundary at any width
export const HALF_DOT = 12; // half of w-6 (24 px outer ring)
export const CORNER_R = 14; // px — SVG corner radius
export const MOBILE_DOT_INSET = 16; // px from edge — matches old left-4
